version: 2

# Source dataset (BigQuery dataset == dbt "schema") naming.
#
# Source YAML is parsed as YAML FIRST, then Jinja is rendered only inside
# string field values, in dbt's LIMITED base context: env_var / var / target /
# modules are available, but PROJECT MACROS ARE NOT (verified: clean_slice is
# undefined here). So we can neither call clean_slice nor use a file-level
# {% set %} block (that breaks YAML parsing). The schema below inlines the SAME
# env-var/target logic as the macro path, as one expression.
#
# MUST stay in sync with the macro path used by seeds/models —
# macros/generate_schema_name.sql -> macros/custom_naming/clean_slice.sql —
# so source() references resolve to where the raw seeds land:
#   PROD     -> RAW
#   non-PROD -> <SLICE>__RAW   (SLICE = sanitised git branch, else PR+run id)

sources:
  - name: ecom
    config:
      # Freshness SLA (TM-AU-01). dbt only evaluates this on `dbt source freshness`
      # (never run in this repo's build/CI) — it documents the contract with upstream.
      # Generous windows because the demo seeds carry historical jafgen dates.
      freshness:
        warn_after: {count: 90, period: day}
        error_after: {count: 365, period: day}
    schema: >-
      {{
        'RAW'
        if (env_var('DBT_ENV_TYPE', target.name) | upper == 'PROD')
        else (
          (modules.re.sub('\W', '_',
            env_var('DBT_CLOUD_GIT_BRANCH',
              env_var('DBT_GIT_BRANCH',
                env_var('GIT_BRANCH', var('git_branch', ''))))) | trim | upper)
          if (modules.re.sub('\W', '_',
                env_var('DBT_CLOUD_GIT_BRANCH',
                  env_var('DBT_GIT_BRANCH',
                    env_var('GIT_BRANCH', var('git_branch', ''))))) | trim | length > 0)
          else ('PR' ~ (modules.re.sub('\W', '_', env_var('DBT_CLOUD_PR_ID', env_var('DBT_PR_ID', ''))) | trim | upper)
                ~ '_RUN' ~ (modules.re.sub('\W', '_', env_var('DBT_CLOUD_RUN_ID', env_var('DBT_RUN_ID', ''))) | trim | upper))
        ) ~ '__RAW'
      }}
    description: E-commerce data for the Jaffle Shop
    tables:
      # Static reference seeds carry no per-row load timestamp, so they use current_timestamp()
      # as loaded_at_field ("always-current" reference data). Event tables use their real event time.
      - name: raw_customers
        description: One record per person who has purchased one or more items
        config:
          loaded_at_field: current_timestamp()
        columns:
          - name: id
            description: Primary key — one row per customer.
            data_tests:
              - not_null
              - unique
      - name: raw_orders
        description: One record per order (consisting of one or more order items)
        config:
          loaded_at_field: ordered_at
        columns:
          - name: id
            description: Primary key — one row per order.
            data_tests:
              - not_null
              - unique
      - name: raw_items
        description: Items included in an order
        config:
          loaded_at_field: current_timestamp()
        columns:
          - name: id
            description: Primary key — one row per order item.
            data_tests:
              - not_null
              - unique
      - name: raw_stores
        description: One record per physical store location
        config:
          loaded_at_field: opened_at
        columns:
          - name: id
            description: Primary key — one row per store location.
            data_tests:
              - not_null
              - unique
      - name: raw_products
        description: One record per SKU for items sold in stores
        config:
          loaded_at_field: current_timestamp()
        columns:
          - name: sku
            description: Primary key — the product SKU.
            data_tests:
              - not_null
              - unique
      - name: raw_supplies
        description: One record per supply per SKU of items sold in stores
        config:
          loaded_at_field: current_timestamp()
        columns:
          # (id, sku) is the natural grain — neither column is unique on its own.
          - name: id
            description: Supply identifier (repeats across the SKUs a supply is used in).
            data_tests:
              - not_null
          - name: sku
            description: The product SKU this supply is an ingredient of.
            data_tests:
              - not_null