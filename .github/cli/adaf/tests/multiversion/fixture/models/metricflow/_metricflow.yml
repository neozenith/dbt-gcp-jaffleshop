version: 2

models:
  - name: metricflow_time_spine
    description: "Day-grain time spine for the semantic layer (outside the data product)."
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        granularity: day
