version: 2

# BROKEN ON PURPOSE: none of these sources declare a freshness SLA (TM-AU-01 blocker each).
sources:
  - name: ecom
    schema: raw
    tables:
      - name: raw_customers
      - name: raw_orders
      - name: raw_supplies
